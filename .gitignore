# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# --- node / common tooling (in case adapters or examples add any) ---
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- bootstrap installer scratch (temp files written during substitution) ---
*.keelwright.tmp.*

# --- Keelwright-managed state in a CONSUMING project (kept out of the framework repo) ---
# The SessionEnd hook writes memory snapshots into a target project, not here; ignored defensively.
.harness/memory-snapshots/

# --- env / secrets (never commit) ---
.env
.env.*
!.env.example
